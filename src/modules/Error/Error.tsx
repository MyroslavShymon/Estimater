import * as React from "react";
import { default as classes } from "../Page.module.scss";

// export interface ErrorProps {}

const Error: React.FC = (): JSX.Element => {
  return <div className={classes.page}>Error</div>;
};

export default Error;
